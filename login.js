const buttonElement = document.querySelector('.js-submit-button');
let user = document.querySelector('.js-user-name');

buttonElement.addEventListener('click', () => {
   window.location.href = "index.html";
});